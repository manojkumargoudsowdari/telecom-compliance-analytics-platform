environment:
  name: local

source:
  endpoint: https://opendata.fcc.gov/resource/3xyp-aqkj.json
  file_format: json
  pagination:
    enabled: true
    method: limit_offset
    page_size: 50000

output:
  raw_path: data/raw/fcc/consumer_complaints
  raw_file_prefix: consumer_complaints_page
  metadata_path: data/raw/fcc/_metadata

run:
  mode: full
  id_strategy: timestamp_utc_compact
  timestamp_format: iso_8601_utc

http:
  request_timeout_seconds: 120
  max_retries: 3
  retry_backoff_seconds: 5

overrides:
  enabled: true

gold:
  daily_output_path: data/gold/fact_complaints_daily
  monthly_output_path: data/gold/fact_complaints_monthly
  metadata_path: data/gold/_metadata
  quality_path: data/gold/_quality
  rolling_window_months: 3
